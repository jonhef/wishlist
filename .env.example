POSTGRES_DB=wishlist
POSTGRES_USER=wishlist
POSTGRES_PASSWORD=wishlist_dev_password
POSTGRES_HOST_PORT=55432

# Optional: override backend connection string for local dotnet run / ef commands.
# ConnectionStrings__WishlistDb=Host=localhost;Port=55432;Database=wishlist;Username=wishlist;Password=wishlist_dev_password

APPLY_MIGRATIONS_ON_STARTUP=true
