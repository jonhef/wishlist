POSTGRES_DB=wishlist
POSTGRES_USER=wishlist
POSTGRES_PASSWORD=wishlist_dev_password
POSTGRES_HOST_PORT=56432
BACKEND_HOST_PORT=19080
FRONTEND_HOST_PORT=5183
NGINX_HTTP_HOST_PORT=8088
NGINX_HTTPS_HOST_PORT=8445

# Optional: override backend connection string for local dotnet run / ef commands.
# ConnectionStrings__WishlistDb=Host=localhost;Port=56432;Database=wishlist;Username=wishlist;Password=wishlist_dev_password

APPLY_MIGRATIONS_ON_STARTUP=true
